[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "factory-runtime"
version = "1.0.0"
description = "Deterministic runtime orchestration engine for the VibeCoding AI Software Factory"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
factory = "factory_runtime.cli:main"
