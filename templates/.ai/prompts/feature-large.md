Read `AGENTS.md`, `requirements.md`, `.ai/specs/*`, `.ai/context/*` and the relevant implementation files.

Then:
1. diagnose the current state
2. split the work into phases or sub-packages
3. identify risks and side effects
4. implement only phase 1 or the first 1-2 sub-packages
5. run a check/build if available
6. list changed files, remaining risks and recommended next step

Do not perform a broad unreviewed rewrite.
