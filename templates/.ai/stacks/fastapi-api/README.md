# FastAPI API Stack Pack

Use this stack for request/response APIs.

## Deterministic defaults
- `app/api/` for routers
- `app/domain/` for entities and business rules
- `app/services/` for use-case orchestration
- `tests/unit`, `tests/integration`, `tests/smoke` for validation
