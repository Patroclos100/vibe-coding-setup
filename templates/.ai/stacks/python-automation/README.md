# Python Automation Stack Pack

Use this stack for scripts, ETL helpers, and scheduled automation.

## Deterministic defaults
- `src/<project_name>/` for implementation
- `tests/unit` for logic
- `tests/smoke` for CLI or end-to-end file-based validation
