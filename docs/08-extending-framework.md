# Extending the Framework

See `docs/contributor-guide.md` for the full extension model.

Short version:
- runtime changes belong under `templates/`
- framework verification changes belong under `framework-tests/`
- stack additions require both `stack.json` and documentation
- schema changes require scenario and documentation updates
